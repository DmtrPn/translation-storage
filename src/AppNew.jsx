import React from 'react';

import ListContainer from './containers/ListContainer';

function AppNew() {
    return (
        <main>
            <ListContainer />
        </main>
    );
}

export default AppNew;